services:
  - type: web
    name: ojt-management-app
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromService:
          name: ojt-database  # This should match your existing database name in Render
          type: postgresql
          property: connectionString
